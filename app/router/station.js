// IMPORTATIONS

import { Router } from 'express';
const router = Router();

// import { fetchAllStations, fetchOneStation, createStation, updateStation, deleteStation } from '../controller/stationController.js';

// ROUTES CHARGING_STATIONS
//CHECK CREER LES CONTROLLERS/MODELS ET DATAMAPPER LIES
// router.get('/stations', fetchAllStations);
// router.post('/stations', createStation);

// router.get('/station/:id', fetchOneStation);
// router.patch('/station/:id', updateStation);
// router.delete('/station/:id', deleteStation);

export { router };